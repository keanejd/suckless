//Modify this file to change what commands output to your statusbar, and recompile using the make command.
static const Block blocks[] = {
	/*Icon*/	/*Command*/		/*Update Interval*/	/*Update Signal*/
	{"  \uefc5  ", "free -h | awk '/^Mem/ { print $3\"/\"$2 }' | sed s/i//g",	30,		0},

	{"", "date '+%b %d (%a) %I:%M%p'",					5,		0},
	{"", "curl -s wttr.in/houston?format=3",                                3600,           0},
        {"","/home/john/suckless/dwmblocks/dwmblocks_scripts/battery_status.sh" , 30, 0},
	{" \uf4bc  ", "vmstat 1 2 | tail -1 | awk '{print 100-$15\"%\"}'", 5, 0},
	{"", "/home/john/suckless/dwmblocks/dwmblocks_scripts/battery_status.sh", 60,0}
};

//sets delimiter between status commands. NULL character ('\0') means no delimiter.
static char delim[] = " | ";
static unsigned int delimLen = 5;
